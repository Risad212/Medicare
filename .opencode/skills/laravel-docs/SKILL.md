---
name: laravel-docs
description: Laravel 11/13 official documentation reference, routing, Eloquent, Blade, middleware, validation, and auth. Use ONLY when user asks for Laravel docs, documentation lookup, or official Laravel reference.
---

# Laravel Docs Skill

Use when user asks for Laravel documentation, official reference, or docs lookup.

## Sources
- Official: https://laravel.com/docs
- API: https://laravel.com/api
- Use `composer show laravel/framework` for installed version (now 13.29.0)

## Quick Reference
- Routing: `Route::get/post`, `Route::middleware`, `Route::resource`
- Eloquent: `Model`, `fillable`, `casts`, `hasMany`, `whereHas`
- Blade: `{{ }}`, `@extends`, `@section`, `@csrf`
- Validation: `validate()`, `FormRequest`, `exists:table,column`
- Auth: `Auth::routes()`, `middleware('auth')`, `Gate`, `Policy`
